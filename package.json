{
  "name": "bulletflowy",
  "private": true,
  "workspaces": [
    "packages/server",
    "packages/client",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w packages/server\" \"npm run dev -w packages/client\"",
    "build": "npm run build -w shared && npm run build -w packages/client && npm run build -w packages/server",
    "start": "node packages/server/dist/index.js",
    "test": "npm test -w packages/server",
    "lint": "tsc --noEmit -p packages/server/tsconfig.json"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "typescript": "^5.7.3"
  }
}
